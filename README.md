# Pathfindr Proposal Assets

Public logo and team image asset pack for the `proposal-page-generator` skill.

Generated from `/Users/dawidnaude/.codex/skills/proposal-page-generator/assets/logo-manifest.json`.

Use the raw GitHub URLs from `logo-manifest.public.json` in proposal microsites so generated pages do not depend on local files.

Team headshots live under `team/` and are referenced from `assets/content-library.json`.
